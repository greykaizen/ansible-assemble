[local]
localhost ansible_connection=local

[infra]
# DNS and Firewall (local demo only, no external APIs)
infra_local ansible_connection=local

[all:vars]
# Python interpreter is set in playbooks to handle paths with spaces

